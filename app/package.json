{
    "name": "email-validator-tests",
    "version": "1.0.0",
    "description": "Unit tests for email validation using Vitest",
    "type": "module",
    "scripts": {
        "test": "vitest",
        "test:ui": "vitest --ui",
        "test:coverage": "vitest --coverage"
    },
    "devDependencies": {
        "vitest": "^1.2.2",
        "jsdom": "^25.0.1"
    }
}